# instagram-poster-assets

Public image host for `instagram-poster`. Instagram's Graph API only accepts media as a fetchable public URL, so rendered post images land here (under `images/<date>/`) before the automation calls the API.

Nothing else lives in this repo — no logic, no secrets, no schedule data. Just images, so keep it that way.
